import React, {useEffect} from "react";
import { api } from "../services/api";

const Main = ({ auth }) => {

  return (
    <div>
      <h1>Suns out, Guns out</h1>
    </div>
  );
};

export default Main;
